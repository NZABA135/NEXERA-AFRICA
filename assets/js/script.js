// Vanilla JS for form validation and other interactions

// Example: Password confirmation on register (if added)
document.addEventListener('DOMContentLoaded', function() {
    const forms = document.querySelectorAll('form');
    forms.forEach(form => {
        form.addEventListener('submit', function(e) {
            // Add custom validations if needed
        });
    });
});

// Language switcher (already handled in PHP, but JS for smooth if needed)